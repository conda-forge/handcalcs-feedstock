{% set name = "handcalcs" %}
{% set version = "1.10.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/handcalcs-{{ version }}.tar.gz
  sha256: 6c4dc51c7309cdaee2eddb79efe13f7d6a7555309035fd10fb0c9bd6ffbff2b4

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - flit
  run:
    - innerscope >=0.7.0
    - more-itertools
    - pyparsing
    - python >={{ python_min }}

test:
  imports:
    - handcalcs
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/connorferster/handcalcs
  summary: Converts your Python calculation script into beautifully rendered Latex,
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - connorferster
    - jan-janssen
