{% set name = "handcalcs" %}
{% set version = "1.7.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/handcalcs-{{ version }}.tar.gz
  sha256: c530e4d98cba7de414b93d860a83297ed7124a62e800dc1dd17cda0d6daf0a30

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
    - flit
  run:
    - innerscope >=0.2.0
    - more-itertools >=8.5.0
    - nbconvert >=6.0.0
    - pyparsing
    - python >=3.7

test:
  imports:
    - handcalcs
    - handcalcs.handcalcs_html
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/connorferster/handcalcs
  summary: Converts your Python calculation script into beautifully rendered Latex,
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
